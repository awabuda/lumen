{
  "name": "@lumen/mcp",
  "version": "0.9.0",
  "description": "Model Context Protocol client: JSON-RPC 2.0 transport for the MCP 2025-03-26 spec, with stdio and Streamable HTTP transports, server discovery, and tool proxy for the Lumen registry.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lumen/core": "workspace:*",
    "@lumen/config": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
