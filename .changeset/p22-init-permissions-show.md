---
'@lumen/cli': minor
---

Add `lumen init [--force] [--path <file>]` to write a starter `~/.lumen/permissions.yaml` (default `ask` + least-privilege rule set), and `lumen permissions show [--path <file>] [--json]` to print the resolved policy in human-readable or JSON form.
