---
'@lumen/cli': minor
---

Add the P21.3 durable-execution bench scenarios under `apps/cli/test/perf/` (step-checkpoint cost, resume latency, concurrent saves, checkpoint size, stale-resume rejection). Opt-in via `LUMEN_BENCH=1`.
