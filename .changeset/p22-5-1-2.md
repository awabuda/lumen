---
'@lumen/core': minor
'@lumen/cli': minor
---

Add the P22.5.1 composition wiring (auto-mode middleware sits between the static permission layer and the interrupt chain when the policy file's `autoMode.enabled` is true) and the P22.5.2 policy file `autoMode:` block (Zod optional, defaults to omitted).
